# 交付成果

## 导出依赖文件

### 1. 导出Conda环境配置 (environment.yml)

bash

```
# 导出当前环境的所有包（包含精确版本）
conda env export > environment.yml

# 更好的做法：不包含构建版本号，提高跨平台兼容性
conda env export --no-builds > environment.yml
```

### 2. 导出pip依赖 (requirements.txt)

bash

```
# 导出所有通过pip安装的包（包含版本号）
pip freeze > requirements.txt

# 或使用更好的格式
pip list --format=freeze > requirements.txt
```

# 项目环境复现指南

## 📋 快速开始

### 方法一：使用Conda一键恢复（推荐）

**克隆项目**

```bash
git clone <项目地址>
cd <项目目录>
conda env create -f environment.yml
conda activate my_project
#验证
python -c "import torch; print(f'PyTorch版本: {torch.__version__}')"
python -c "import pandas; print(f'Pandas版本: {pandas.__version__}')"
```

### 方法二：手动安装步骤

1. **创建基础环境**

bash

```
conda create -n my_project python=3.9 -y
conda activate my_project
```

2. **安装Conda包**

bash

```
conda install numpy pandas scikit-learn matplotlib jupyter -y
```

3. **安装Pip包**

bash

```
pip install -r requirements.txt
```
